Convert the supplied reference image into a black-and-white Roman mosaic in the style of the 2nd-century pavements at Ostia Antica and the Baths of Caracalla — pure monochrome figurative and ornamental work.

Style:
- Only two tessera colors: jet black basalt and bone-white limestone. Each tessera is filled with a SINGLE SOLID UNIFORM COLOR (pure black or pure white) — no inner texture, no gradient, no gray pixels inside a tile.
- Tesserae roughly 8-10 mm square, laid in tight straight rows in background fields and in curving *vermiculatum* lines around the silhouettes of figures and shapes. The TILE SIZE (8-10 mm) is the SAME in background fields and in figure silhouettes — only the layout direction changes (straight rows vs curving lines). Background tesserae must NOT be larger or coarser than figure tesserae. Self-check: if a background tile is visibly larger than a figure tile, you have failed.
- Form is rendered as bold black silhouette against a white field (or the inverse), with internal detail picked out as fine black lines of single-tessera width.
- Materials: black basalt + cream limestone only. No glass, no terracotta.
- Grout: a clearly visible CONTINUOUS line on every side of every tile, at least 4 pixels wide in the output, unbroken. Since the tiles themselves are black and white, use a MID-GRAY grout (#666 to #888) so it stays visible against both tile colors — NOT pale lime, NOT hairline. Adjacent tiles never touch.

Subject simplification — hair, fur, fabric folds, and other fine line-textures:
- When the reference image contains hair (especially long flowing hair such as a woman's hair), fur, or similar fine line-textures: DO NOT try to render every strand with extra-tiny tesserae. Simplify these areas into a small number (2-3) of broad regions — solid black silhouette with a few internal lines of single-tessera width for the main flow direction.
- The tesserae inside hair regions must be the SAME SIZE as the tesserae elsewhere in the mosaic. NEVER use smaller tesserae just to capture more strand detail.

Camera and lighting:
- Strict orthographic top-down view, no perspective.
- Flat, even, diffuse lighting; no directional shadows.
- Mosaic fills the frame edge-to-edge.

Composition:
- Translate the reference image into bold black-and-white silhouettes. Color information from the reference becomes either solid black or solid white based on luminance and visual importance. Preserve subject and proportions.

Output:
- Square 4K (3840 × 3840 px). Clean, sharp, detection-friendly: solid uniform tile colors (pure black or pure white only), mid-gray continuous grout, crisp boundaries.
